## OOP Challenge 2

This folder contains a set of introductory OOP activities in which I learned concepts regarding encapsulation, access modifiers, attribute access methods (getters), attribute modification methods (setters), the use of the `import` and `this` keywords, and best practices for organizing code into packages.